{
  "name": "@personal-ai-workspace/desktop",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "prepare:toolbox": "node scripts/prepare-local-toolbox.mjs",
    "dev": "npm run prepare:toolbox && tauri dev",
    "build": "npm run prepare:toolbox && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "esbuild": "0.25.10"
  }
}
